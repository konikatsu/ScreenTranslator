<Window x:Class="ScreenTranslator.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings - Screen Translator" Height="300" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#1E1E24" Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Gemini API Settings" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" Grid.Row="0"/>

        <TextBlock Text="API Key:" FontSize="14" Margin="0,0,0,5" Grid.Row="1"/>
        <PasswordBox x:Name="PwdApiKey" Height="30" Margin="0,0,0,15" Grid.Row="2"
                     Background="#2D2D35" Foreground="White" BorderBrush="#3A3A45"
                     Padding="5"/>

        <TextBlock Text="Model:" FontSize="14" Margin="0,0,0,5" Grid.Row="3"/>
        <TextBox x:Name="TxtModel" Height="30" Margin="0,0,0,20" Grid.Row="4" VerticalAlignment="Top"
                 Background="#2D2D35" Foreground="White" BorderBrush="#3A3A45"
                 Padding="5"/>

        <TextBlock Grid.Row="4" VerticalAlignment="Bottom" TextWrapping="Wrap" Margin="0,0,0,15"
                   Foreground="#AAAAAA" FontSize="12">
            注意: 「AI解説機能 (Alt + W)」を実行すると、選択した範囲のスクリーンショットがGoogle Gemini APIへ送信されます。
        </TextBlock>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnCancel" Content="Cancel" Width="80" Height="30" Margin="0,0,10,0"
                    Background="#3A3A45" Foreground="White" BorderThickness="0" Click="BtnCancel_Click"/>
            <Button x:Name="BtnSave" Content="Save" Width="80" Height="30"
                    Background="#00D2FF" Foreground="Black" FontWeight="Bold" BorderThickness="0" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
