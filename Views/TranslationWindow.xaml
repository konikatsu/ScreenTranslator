<Window x:Class="ScreenTranslator.Views.TranslationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Translation"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        MinWidth="280"
        MaxWidth="480"
        MinHeight="100"
        MaxHeight="400"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <DropShadowEffect x:Key="CardShadow"
                          BlurRadius="20"
                          ShadowDepth="4"
                          Opacity="0.5"
                          Color="#000000"/>
    </Window.Resources>

    <Border Margin="15"
            Background="#1E1E24"
            BorderBrush="#3D3D48"
            BorderThickness="1"
            CornerRadius="12"
            Effect="{StaticResource CardShadow}">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🌐" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="Screen Translator"
                               FontWeight="SemiBold"
                               FontSize="13"
                               Foreground="#00D2FF"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnCopy"
                            Content="📋 コピー"
                            Click="BtnCopy_Click"
                            Background="#2D2D38"
                            Foreground="#E0E0E0"
                            BorderThickness="0"
                            Padding="8,3"
                            FontSize="11"
                            Cursor="Hand"
                            Margin="0,0,6,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button x:Name="BtnClose"
                            Content="✕"
                            Click="BtnClose_Click"
                            Background="Transparent"
                            Foreground="#888888"
                            BorderThickness="0"
                            Padding="6,2"
                            FontSize="12"
                            FontWeight="Bold"
                            Cursor="Hand"/>
                </StackPanel>
            </Grid>

            <!-- Main Translated Content -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          MaxHeight="200">
                <TextBlock x:Name="TxtTranslation"
                           Text="翻訳中..."
                           FontSize="15"
                           FontWeight="Medium"
                           Foreground="#FFFFFF"
                           TextWrapping="Wrap"
                           LineHeight="22"/>
            </ScrollViewer>

            <!-- Source English Text (Secondary) -->
            <Border Grid.Row="2"
                    Margin="0,12,0,0"
                    Padding="8"
                    Background="#16161A"
                    CornerRadius="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0"
                               Text="原文 (OCR):"
                               FontSize="10"
                               Foreground="#70707D"
                               Margin="0,0,0,2"/>
                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  MaxHeight="70">
                        <TextBlock x:Name="TxtOriginal"
                                   Text=""
                                   FontSize="12"
                                   Foreground="#9E9EA8"
                                   TextWrapping="Wrap"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
